export const styles = (theme) => {
  return {
    margin: {
      margin: theme.spacing(2)
    },
    padding: {
      padding: theme.spacing(1)
    },
    paperRoot: {
      padding: theme.spacing(1),
      margin: theme.spacing(2),
      width: '30%',
    }
  }
}
